{
  "name": "claude-code-bridge",
  "version": "1.0.0",
  "description": "A lightweight bridge that exposes Claude Code CLI over HTTP, with optional WebSocket relay support for remote access.",
  "main": "lib/bridge.js",
  "bin": {
    "claude-code-bridge": "bin/cli.js"
  },
  "scripts": {
    "start": "node bin/cli.js start"
  },
  "keywords": ["claude", "cli", "bridge", "relay", "websocket"],
  "license": "MIT",
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
